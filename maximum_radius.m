function res = maximum_radius(X)
%MAXIMUM_RADIUS Calculates an upper limit for integrals with rho as an
%integrand, given X parameters

% Should not depend on rho_0, only a & b
a = X(2); b = X(3);

% res = f(a,b)
end

